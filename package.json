{
  "name": "conciliacion-cobros",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Motor de conciliación HIOPOS ↔ Mercado Pago / Payway (multi-tenant)",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:deploy": "prisma migrate deploy",
    "db:studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "5.22.0",
    "csv-parse": "^7.0.0",
    "dotenv": "^17.4.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "prisma": "5.22.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
